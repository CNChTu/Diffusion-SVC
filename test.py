# original_dict = {"2": 0.8, "3": 0.2, "4": 0.3333}
# converted_dict = {int(key): value for key, value in original_dict.items()}

# print("{" + ", ".join(f"{key}: {value}" for key, value in converted_dict.items()) + "}")

# def test(i):
#     i += 1
#     return i
# print(0, test(0))